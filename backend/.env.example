# Backend (Node/Express)
PORT=3000
NODE_ENV=production

# URL of the Python Wordle bot service
# Local dev (docker-compose): http://wordle-bot:5000
# Railway: use the private networking URL Railway provides when services are linked
WORDLE_BOT_URL=http://wordle-bot:5000

# Python Flask service
# PORT is also read by wordle_flask_app.py